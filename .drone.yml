build:
    build_starting:
        image: python:2
        commands:
            - echo "Beginning new build"

test:
    run_tests:
        image: python:2-onbuild
        commands:
            - ansible-playbook -i inventory main.yml

